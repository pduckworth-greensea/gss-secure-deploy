[defaults]
inventory = localhost,
host_key_checking = False
remote_user = gssadmin
private_key_file = ~/.ssh/gss_maas
timeout = 30

[privilege_escalation]
become = True
become_method = sudo
become_user = root
